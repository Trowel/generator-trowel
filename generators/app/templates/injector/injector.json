{
  <% if (props.scss) { %>"stylevendors": [
    "@bower/trowel-<%= props.name %>/<%= folders.src %>/scss/<%= props.name %>.scss"
  ],<% } %>
  <% if (props.css) { %>"stylevendors": [
    "@bower/trowel-<%= props.name %>/<%= folders.src %>/css/<%= props.name %>.css"
  ],<% } %>
  <% if (props.javascript) { %>"scriptvendors": [
    "@bower/trowel-<%= props.name %>/<%= folders.dest %>/javascript/<%= props.name %>.min.js"
  ],<% } %>
  "stylegui": [
    "@bower/trowel-<%= props.name %>/<%= folders.gui %>/<%= props.name %>-gui.scss"
  ],
  "templategui": [
    "@bower/trowel-<%= props.name %>/<%= folders.gui %>/<%= props.name %>-gui.html.twig"
  ]
}
